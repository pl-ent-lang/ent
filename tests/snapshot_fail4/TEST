// main: B
// Test: Checks that snapshot fails at runtime if the snapshotted object returns a mode type
// outside the snapshot bounds when: 
//  (1) a method mode type variable is used for both bounds
//  (2) a class mode type variable is substituted into said method
