// Test: Checks that the resulting existential from a snapshot conforms to
// the waterfall invariant when using a class mode type variable as its bound
// that is not the class's mode type and bounded by each other.
